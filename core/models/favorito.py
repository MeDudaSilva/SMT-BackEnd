# from django.db import models

# from .user import User


# class Favorito(models.Model):
#     nome = models.ForeignKey(User, on_delete=models.PROTECT, related_name="nomes", null=True, blank=True)


#     def __str__(self):
#         return f"id: {self.id} | nome do seu favorito: {self.nome}"
